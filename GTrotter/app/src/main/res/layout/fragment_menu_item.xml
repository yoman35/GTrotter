<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/recycler_view_row_height"
    android:orientation="horizontal">

    <!-- Icon -->
    <ImageView
        android:id="@+id/recycler_view_row_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="@dimen/activity_vertical_margin"
        android:contentDescription="@string/recycler_view_icon_description"
        android:src="@drawable/ic_g" />

    <!-- Title -->
    <TextView
        android:id="@+id/recycler_view_row_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="@dimen/recycler_view_text_left_margin_from_icon"
        android:text="@string/recycler_view_title" />

</LinearLayout>

